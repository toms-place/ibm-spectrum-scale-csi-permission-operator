# ibm-spectrum-scale-csi-permission-operator

This k8s operator changes permissions of a recently bound spectrum scale PV.

## WIP

- select permissions dynamically
- select name of storageclass on initialization
